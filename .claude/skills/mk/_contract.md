# contract

- the sketch contains one box per layer
- each surface api entrypoint has at least one protocol sketch

```md:form:contract
{skills.mk.sketch(architecture)}

{...skills.mk.sketch(protocol)[]}
```
