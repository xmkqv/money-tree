
delegate-constructs(layer)
  enumerate every callable, class, and type
  deeply scan declared dependencies and workspace packages for the same role
  log candidates and evidence
  skills.search(docs)
  log report
  wait for feedback
  if approved:
    replace confirmed reinventions with package apis
    remove replaced code and imports
    log coverage, replacements, and rejections
