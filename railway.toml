[build]
builder = "DOCKERFILE"
watchPatterns = ["/src/bot/**", "/src/cli/**", "/pyproject.toml", "/uv.lock", "/Dockerfile", "/railway.toml"]

[deploy]
startCommand = 'uv run --locked --no-sync mt trade --strategy "$STRATEGY"'
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 10
